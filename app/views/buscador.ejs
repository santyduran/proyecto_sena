<!DOCTYPE html>
<html lang="es">
<head>
 
<meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Iniciar sesion</title>
    <link rel="stylesheet" href="style.css">
    <script src="index.js" defer></script>
</head>
<body>
  <main class="full-page">
    <div class="form-container">

        <form id="formBuscar" class="buscador"  class="buscador" method="get" action="/">
        <input type="text" id="inputCiudad" name="filter" placeholder="Buscar ciudad o destino...">
       <button type="submit"><i class="fas fa-search"></i> Buscar</button>
      </form>
      <h2>🌍 Turismo en Colombia</h2>
      <p>Encuentra hoteles, restaurantes y actividades turísticas en esta ciudad.</p>

      <!-- Hoteles -->
      <!-- Hoteles -->
        <section>
        <h3><i class="fas fa-hotel"></i> Hoteles</h3>
        <ul id="listaHoteles">
            <% hoteles.forEach(hotel => { %>
            <li>
                <strong><%= hotel.nombre %></strong> - ⭐ <%= hotel.estrellas %><br>
                Dirección: <%= hotel.direccion %><br>
                Tel: <%= hotel.telefono %><br>
                <!-- <img src="<%= hotel.image %>" alt="Imagen de <%= hotel.nombre %>" style="width:200px; height:auto;"><br> -->
                <!-- Botón reservar -->
                <a href="/reservar/hotel/<%= hotel.id %>" class="btn-reservar">Reservar</a>
            </li>
            <% }) %>
        </ul>
        </section>

        <!-- Restaurantes -->
        <section>
        <h3><i class="fas fa-utensils"></i> Restaurantes</h3>
        <ul id="listaRestaurantes">
            <% restaurantes.forEach(restaurante => { %>
            <li>
                <strong><%= restaurante.nombre %></strong> - 🍴 <%= restaurante.tipo_cocina %><br>
                Dirección: <%= restaurante.direccion %><br>
                Horario: <%= restaurante.horario_atencion %><br>
                <!-- Botón reservar -->
                <a href="/reservar/restaurante/<%= restaurante.id %>" class="btn-reservar">Reservar</a>
            </li>
            <% }) %>
        </ul>
        </section>

        <!-- Actividades -->
        <section>
        <h3><i class="fas fa-hiking"></i> Actividades</h3>
        <ul id="listaActividades">
            <% actividades.forEach(act => { %>
            <li>
                <strong><%= act.nombre %></strong> - <%= act.tipo_actividad %><br>
                Dirección: <%= act.direccion %><br>
                Horario: <%= act.horario %> | Precio: $<%= act.precio %><br>
                <!-- Botón reservar -->
                <a href="/reservar/actividad/<%= act.id %>" class="btn-reservar">Reservar</a>
            </li>
            <% }) %>
        </ul>
        </section>


      
     
      <div id="logoutDiv" style="display: none;">
           <button id="cerrar_session" style="margin-top: 15px; background: #c0392b;">Cerrar sesión</button>
       </div>
       <div id="loginDiv" style="display: none;">
           <button id="iniciar_session" style="margin-top: 15px; background: #c0392b;">Iniciar sesión</button>
       </div>
    </div>
  </main>
</body>
</html>
